name: HOPE Platform Tests

on:
  push:
    branches: [main]
  schedule:
    - cron: '0 8 * * *'
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Playwright
        run: |
          npm init -y
          npm install playwright
          npx playwright install chromium --with-deps

      - name: Run Tests
        id: run_tests
        run: node tests/hope.test.js
        continue-on-error: true

      - name: Show Results
        run: |
          echo '## HOPE Platform Test Results' >> $GITHUB_STEP_SUMMARY
          cat evolution/last-results.json >> $GITHUB_STEP_SUMMARY
        continue-on-error: true

      - name: Update Memory
        run: node evolution/memory-engine.js
        continue-on-error: true

      - name: Commit Memory Update
        run: |
          git config --global user.name 'hope-test-engine'
          git config --global user.email 'tests@hopenetwork.com'
          git remote set-url origin https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/chuckedwards1972/polr-webapp.git
          git add evolution/
          git diff --staged --quiet || git commit -m 'Test run memory update'
          git push
        continue-on-error: true

      - name: Fail if tests failed
        if: steps.run_tests.outcome == 'failure'
        run: exit 1